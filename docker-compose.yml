
version: '3.8'

services:
  xx-job:
    build: .
    container_name: xx-job
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
